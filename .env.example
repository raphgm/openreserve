# Local secrets for `make gateway`. Copy to .env (chmod 600). Never commit .env.
# Use test-mode keys only until a licensed custody partner is in place.
PAYSTACK_SECRET_KEY=
FLW_SECRET_KEY=
FLW_SECRET_HASH=
# SMS codes for phone sign-in (leave empty for dev mode: codes go to the log)
TERMII_API_KEY=
TERMII_SENDER=ORPay
